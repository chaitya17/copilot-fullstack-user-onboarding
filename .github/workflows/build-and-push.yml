name: Build and Push Images

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  release:
    types: [ published ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        component: [backend, frontend]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Container Registry
      uses: docker/login-action@v2
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.component }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./${{ matrix.component }}
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  # Optional: Deploy to staging environment after successful build
  deploy-staging:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: staging

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # TODO: Add deployment steps for your staging environment
    # Examples:
    # - Deploy to Azure Container Instances
    # - Deploy to Kubernetes cluster
    # - Update Azure App Service container settings

    - name: Placeholder for staging deployment
      run: |
        echo "Staging deployment would happen here"
        echo "Configure this step based on your target platform:"
        echo "- Azure: Use azure/webapps-deploy@v2"
        echo "- Kubernetes: Use azure/k8s-deploy@v1"
        echo "- AWS: Use aws-actions/amazon-ecs-deploy-task-definition@v1"

# Required secrets for this workflow:
# - GITHUB_TOKEN (automatically provided)
#
# Optional secrets for cloud deployments:
# - AZURE_CREDENTIALS (for Azure deployments)
# - AZURE_SUBSCRIPTION_ID
# - AZURE_RESOURCE_GROUP
# - AWS_ACCESS_KEY_ID (for AWS deployments)
# - AWS_SECRET_ACCESS_KEY
# - KUBECONFIG (for Kubernetes deployments)
