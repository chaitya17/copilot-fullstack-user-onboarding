# Backend API Configuration
# Set this to your backend service URL
VITE_API_URL=http://localhost:8080

# Environment
VITE_NODE_ENV=development

# Optional: Enable debug logging
VITE_DEBUG=true

# CORS Note:
# The backend service should be configured to allow requests from this frontend origin.
# In production, update VITE_API_URL to match your deployed backend service.
