# Local Development Environment Variables
# Copy this file to .env.local and fill in the actual values

# Database Configuration
DB_TYPE=mssql          # or "oracle"

# MSSQL Configuration (default for local dev)
MSSQL_SA_PASSWORD=YourStrong!Passw0rd
JDBC_URL=jdbc:sqlserver://mssql:1433;databaseName=useronboard;encrypt=false;trustServerCertificate=true

# Oracle Configuration (alternative - uncomment when using Oracle)
# ORACLE_JDBC_URL=jdbc:oracle:thin:@oracle:1521/XEPDB1
# ORACLE_SYS_PASSWORD=YourOraclePassword123

# JWT Configuration
# TODO: Generate your own RSA key pair and place in ./keys/ directory
# Use: openssl genrsa -out private_key.pem 2048
#      openssl rsa -in private_key.pem -pubout -out public_key.pem
JWT_PUBLIC_KEY_PATH=/app/keys/public_key.pem
JWT_PRIVATE_KEY_PATH=/app/keys/private_key.pem

# Message Queue Configuration
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/

# Frontend Configuration
FRONTEND_API_URL=http://localhost:8080

# Spring Boot Profile
SPRING_PROFILES_ACTIVE=local

# Production/Cloud Configuration Placeholders
# Uncomment and configure for production deployments
# AZURE_KEY_VAULT_URL=https://your-keyvault.vault.azure.net/
# AZURE_CLIENT_ID=your-service-principal-client-id
# AZURE_CLIENT_SECRET=your-service-principal-secret
# AZURE_TENANT_ID=your-azure-tenant-id

# Database URLs for production (managed services)
# PROD_JDBC_URL=jdbc:sqlserver://your-azure-sql.database.windows.net:1433;database=useronboard
# PROD_ORACLE_URL=jdbc:oracle:thin:@your-oracle-cloud-instance:1521/servicename
